<!-- Attribute (thuộc tính) là một thông tin hoặc mô tả được gắn kết với một đối tượng hoặc một phần tử trong một hệ thống. Thuộc tính định nghĩa các đặc điểm, tính chất hoặc thông tin về một đối tượng và cho phép chúng ta tùy chỉnh và quản lý các thuộc tính đó.
Trong lập trình, thuộc tính có thể áp dụng cho các đối tượng, lớp, phương thức hoặc trường. Chúng cung cấp các thông tin bổ sung về đối tượng và cho phép các hành vi hay tính chất của đối tượng được định nghĩa và tùy chỉnh.

Ví dụ, trong ngôn ngữ lập trình Java, chúng ta có thể sử dụng các Annotation làm các thuộc tính. Annotation cho phép chúng ta áp dụng các thông tin bổ sung cho các lớp, phương thức hoặc trường, để xác định các quy tắc kiểm tra, xác thực, hoặc các thông tin metadata khác. -->
